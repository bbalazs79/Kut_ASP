@{
    ViewData["Title"] = "Login Page";
}
<body>
    <main class="container">

        <section class="login-container" id="animate">



            <form class="form-login">

                <label class="login-title">
                    <h1>Login to your account</h1>
                </label>
                <div class="div-login">


                    <label for="validationServer01">Email</label>
                    <input type="email" class="form-control is-valid" id="validationServer01" placeholder="email@email.com" required>


                    <label for="validationServer02">Password</label>
                    <input type="password" class="form-control is-valid" id="validationServer02" placeholder="password" required>


                    <div class="button_container">
                        <button class="btn btn-primary" type="submit" onclick="SingIn()"><i class="fas fa-sign-in-alt"></i>Sign in</button>
                        <p class="button_container_text">Don't you have an account?<span> <a href="Registration">Click here</a></span> to registration</p>
                    </div>
                </div>

            </form>

        </section>

    </main>
</body>


<script>
    var currentUser;
    function returnData(param) {
        currentUser = param;
        console.log(currentUser);
    }

    function SelectUser(email, callback) {
        $.post('/User/SelectUser', { email: email }, function (data) {
            callback(data);
        });
    }

    function SingIn() {
        SelectUser($('#Email').val(), returnData);
        if (currentUser.Email != null && currentUser.Email == $('#email').val()) {
            alert("Email is not valid!");
            if (currentUser.Password == $('#password')) {
                alert("Password is not valid!");
            }
        } else {
            //Márk ide írd a SignUP/SignIn eltüntetését és a LogOut megjelenítését. 
            //A currentUser tartalmazza a bejelentkezett felhasználó adatait amennyiben helyes a bejelentkezés
            /*Tartalma: Figyelj a nagy betűkre!!!!
             * currentUser.Email
             * currentUser.Password
             * currentUser.LastName
             * etc...
             */

        }
    }
</script>